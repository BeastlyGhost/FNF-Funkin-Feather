package funkin.objects.ui.fonts;

/**
	a `Alphabet` extension, for dialogue boxes
**/
class DialogueAlphabet extends Alphabet
{
	var speed:Float = 1;
	var paused:Bool = false;
	var sounds:Array<String> = ['GF_1', 'GF_2', 'GF_3'];
}
